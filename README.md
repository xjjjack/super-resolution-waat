# super-resolution-waat

## Commit with lint uisng git hooks
Install required package for pre-commit with
```
pip install -r requirements.txt
```
Then run
```
pre-commit install
```
to install the git hooks.

## Checklist
- [ ] TBD